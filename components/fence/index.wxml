<view class="container">
    <view class="title">{{fence.title}}</view>
    <view class="row">
        <block wx:for="{{fence.cells}}" wx:key="id">
            <s-cell cell="{{item}}" x="{{x}}" y="{{index}}" class="cell"></s-cell>
        </block>
    </view>
    <view class="hr"></view>
</view>
