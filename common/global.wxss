.rect-hover {
    position: relative;
    top: 3px;
    left: 3px;
    box-shadow: 0 0 8px rgba(0, 0, 0, .1) inset;
}

.small-hover {
    opacity: .9;
    transform: scale(0.95, 0.95);
}

.medium-hover {
    opacity: .8;
    transform: scale(0.85, 0.85);
}
